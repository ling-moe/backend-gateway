logging:
  level:
    cn.lingmoe.gateway: ${LOGGING_LEVEL:debug}
    org.springframework.cloud: debug
management:
  endpoint:
    health:
      show-details: always
  health:
    redis:
      enabled: false